export enum ResponseMessage {
  SUCCESS = 'Request Successful',
  FAILED = 'Request Failed',
}

export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}
